//Funciones

(
a = {
	arg num1, num2;
	var resultado;
	resultado = num1 + num2
}
)

a.(3,100) //Primero está el nombre de la variable que almacena la función y luego un punto para ejecutar dicha función. Luego los paréntesis contienen los argumentos. 